<p style="color: green"><%= notice %></p>

<h1>Mining types</h1>

<table> 

  <thead>
    <tr>
      <th>Descrição</th>
      <th>Acrônimo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @mining_types.each do |mining_type| %>
      <tr>
        <td><%= mining_type.description %></td>
        <td><%= mining_type.acronym %></td>
        <td><%= link_to t('links.show'), mining_type %></td>
        <td><%= link_to t('links.edit'), edit_mining_type_path(mining_type) %></td>
        <td><%= link_to t('links.delete'), mining_type, method: :delete, data: { confirm: 'Tem certeza'} %></td>
      </tr>
     <% end %> 
  </tbody>
</table>


<%= link_to "New mining type", new_mining_type_path %>
<%= link_to "Back to home", welcome_index_path %>
